version: 0.0
os: linux

files:
  - source: /
    destination: /home/ubuntu/Spoteditor-Backend
    file_exists_behavior: OVERWRITE

permissions:
  - object: /
    owner: ubuntu
    group: ubuntu

hooks:
  ApplicationStart:
    - location: scripts/start.sh
      timeout: 180
      runas: ubuntu